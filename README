# Microsoft Graph Mail Client

Python скрипт для работы с электронной почтой через Microsoft Graph API с OAuth-аутентификацией.

## Возможности

- 📧 **Отправка писем** - отправка электронных писем
- 📥 **Чтение входящих** - просмотр списка писем во входящих
- 👀 **Просмотр содержимого** - чтение полного содержимого писем
- 🗑️ **Удаление писем** - полное удаление или перемещение в корзину
- 🔍 **Поиск писем** - поиск по содержимому писем

## Требования

- Python 3.6+
- Библиотеки: `msal`, `requests`, `bs4`

Установите зависимости:
```bash
pip install msal requests bs4
```
Если вы используете `requirements.txt`, выполните:
```bash
pip install -r requirements.txt
```
## Настройка приложения в Azure Portal:

1. Зарегистрируйте новое приложение в [Azure Portal](https://portal.azure.com/), В EntraID.
2. Получите `CLIENT_ID` и `TENANT_ID`.
3. Настройте разрешения API для Microsoft Graph:
   - Mail.Read
   - Mail.Send
   - Mail.ReadWrite 

## Использование

1. Запустите скрипт:
   ```bash
   python mail_client.py
   ```
2. Следуйте инструкциям для аутентификации и использования функций.

## Лицензия
Этот проект лицензирован под лицензией MIT.